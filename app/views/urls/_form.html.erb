<%= simple_form_for(@url, html: {class: 'ui form'}) do |f| %>
  <%= render "shared/errors", target: @url %>
  <%= f.input :title %>
  <%= f.input :url %>
  <%= f.input :privacy, collection: Url.privacies_map, as: :radio_buttons %>

  <%= f.submit 'Create tracking url' %>
<% end %>
